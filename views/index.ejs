<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/images/icon.png" />
    <title>Notes</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="manifest" href="/manifest.json">
  </head>
  <body>
    <div class="main w-full min-h-screen p-5 bg-zinc-900">
      <h1 class="text-4xl px-4 lg:px-10 py-4 text-[#cdcabb] font-bold">
        Makes your notes easy and effectively
      </h1>
      <div class="form px-4 lg:p-10 text-white">
        <form action="/create" method="post">
          <input
            class="block w-full rounded-md outline-none px-5 py-2 bg-zinc-800 text-[#cacbbb] placeholder-[#cacbbb]/60"
            type="text"
            placeholder="Title goes here"
            name="title"
          />
          <textarea
            class="block w-full rounded-md outline-none px-5 py-2 mt-2 resize-none bg-zinc-800 text-[#cacbbb] placeholder-[#cacbbb]/60"
            placeholder="Write your notes here"
            name="details"
          ></textarea>
          <input
            class="mt-2 font-bold px-5 py-2 rounded-md bg-[#cacbbb] text-[#101010]"
            type="submit"
            value="Create Notes"
          />
        </form>
      </div>
      <h1 class="text-4xl py-4 px-4 lg:px-10 text-[#cdcabb] font-bold">
        All your previous notes
      </h1>
      <div class="tasks px-4 lg:p-10 flex gap-3 p-10 flex-wrap">
        <% if(files.length>0){ %> <% files.forEach(function(val){ %>
        <div class="task min-w-64 rounded-md bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter p-4"><%= val.replace('.txt', '') %></h1>
          <div class="flex flex-row justify-between">
            <form action="/delete" method="post" class="inline">
              <input
                type="hidden"
                name="title"
                value="<%= val.replace('.txt', '') %>"
              />
              <button
                type="submit"
                class="font-bold text-[#101010] bg-[#b44522] px-4 w-full border-2 border-zinc-900 b rounded-md py-2 mr-2"
              >
                Delete
              </button>
            </form>
            <a
              class="font-bold inline-block py-2 px-4 bg-[#cdcabb] rounded-md w-full text-[#101010] border-2 border-zinc-900"
              href="/file/<%= val %>"
              >Read More</a
            >
          </div>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-600">no task here.</h3>
        <% } %>
      </div>
    </div>
    <script>
      if ('serviceWorker' in navigator) {
        window.addEventListener('load', function() {
          navigator.serviceWorker.register('/sw.js');
        });
      }
    </script>
  </body>
</html>
